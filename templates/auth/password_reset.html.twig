{% extends "base.html.twig" %}

{% block title %} Mot de passe oublié ? {% endblock %}

{% block body %}

    <div class="flex h-full bg-white rounded overflow-hidden shadow-lg border-b-8 border-teal-500 mb-5 justify-center flex-1">
        <div class="lg:w-1/2 xl:w-5/12 p-6 sm:p-12 lg:py-24">
            <div class=" flex flex-col items-center">
                <h1 class="font-bold text-4xl leading-tight">Mot de passe oublié ?</h1>
                <p class="text-gray-600  mt-4">Entrez simplement votre adresse e-mail ci-dessous et nous vous enverrons un nouveau mot de passe!</p>
                <div class="w-full flex-1 mt-8">
                    <div class="">
                        <form class="w-full max-w-lg mt-3">
                            <div class="flex flex-wrap -mx-3 mb-3">
                                <div class="w-full px-3">
                                    <input class="py-4 appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white" type="email" placeholder="Email">
                                </div>
                            </div>
                            <button class="mt-5 tracking-wide font-semibold bg-teal-500 text-gray-100 w-full py-4 rounded hover:bg-teal-700 transition-all duration-300 ease-in-out flex items-center justify-center focus:shadow-outline focus:outline-none">
                                <span class="ml-3">Réinitialiser le mot de passe</span>
                            </button>
                        </form>
                        <p class="mt-6 text-xs text-gray-600 text-center">
                            Créer un compte SnowTricks ?
                            <a href="{{ path('auth_register') }}" class="border-b border-gray-400 border-dotted text-gray-500 transition-all duration-300 ease-in-out hover:text-gray-600">
                                S'inscrire !
                            </a>
                        </p>
                        <p class="mt-2 text-xs text-gray-600 text-center">
                            Vous avez déjà un compte ?
                            <a href="{{ path('auth_login') }}" class="border-b border-gray-400 border-dotted text-gray-500 transition-all duration-300 ease-in-out hover:text-gray-600">
                                S'identifier !
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </div>
        <div class="flex-1 bg-indigo-100 text-center hidden lg:flex">
            <div class=" w-full bg-contain bg-center bg-no-repeat" style="background-image: url('https://storage.jukeboxprint.com/s/images/public/email/forgot-password.png');"></div>
        </div>
    </div>

{% endblock %}
