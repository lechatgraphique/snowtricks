{% extends "base.html.twig" %}

{% block title %} Se connecter à SnowTricks {% endblock %}

{% block body %}
    <div class="flex h-full bg-white rounded overflow-hidden shadow-lg border-b-8 border-teal-500 mb-5 justify-center flex-1">
        <div class="lg:w-1/2 xl:w-5/12 p-6 sm:p-12 lg:py-24">
            <div class=" flex flex-col items-center">
                <h1 class="font-bold text-4xl leading-tight">Se connecter à SnowTricks</h1>
                <p class="text-gray-600  mt-4">Merci de revenir sur SnowTricks, accédez à la meilleure figures de snowboard 🙃</p>
                <div class="flex w-full mt-4 border-b-4 border-gray-300">
                    <a href="{{ path('auth_register') }}" class="text-xl p-3 border-b-4 -mb-1 transition-all duration-300 ease-in-out hover:text-teal-500 hover:border-teal-500">Inscription</a>
                    <a href="{{ path('auth_login') }}" class="text-xl p-3 border-b-4 -mb-1 text-teal-500 border-teal-500">Login</a>
                </div>
                    <form action="{{ path('auth_login') }}" method="post" class="w-full max-w-lg mt-8">
                        <div class="flex flex-wrap -mx-3 mb-3">
                            <div class="w-full px-3">
                                <input required name="_username" class="py-4 appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white" type="email" placeholder="Email">
                            </div>
                        </div>
                        <div class="flex flex-wrap -mx-3 mb-3">
                            <div class="w-full px-3">
                                <input required name="_password" class="py-4 appearance-none block w-full bg-gray-200 text-gray-700 border border-gray-200 rounded py-3 px-4 mb-3 leading-tight focus:outline-none focus:bg-white"  type="password" placeholder="Mot de passe">
                            </div>
                        </div>
                        <button class="mt-5 tracking-wide font-semibold bg-teal-500 text-gray-100 w-full py-4 rounded hover:bg-teal-700 transition-all duration-300 ease-in-out flex items-center justify-center focus:shadow-outline focus:outline-none">
                            <span class="ml-3">LOGIN</span>
                        </button>
                    </form>
                     <p class="mt-6 text-xs text-gray-600 text-center">
                        Mot de passe oublié ?
                        <a href="{{ path('auth_password_reset') }}" class="border-b border-gray-400 border-dotted text-gray-500 transition-all duration-300 ease-in-out hover:text-gray-600">
                            C'est par ici :)
                        </a>
                    </p>
            </div>
        </div>
        <div class="flex-1 bg-indigo-100 text-center hidden lg:flex">
            <div class=" w-full bg-cover bg-center" style="background-image: url('https://images.unsplash.com/photo-1549911265-e9e6437ae7a9?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1876&q=80');"></div>
        </div>
    </div>

{% endblock %}
