{% if app.request.attributes.get('_route') == 'admin.categories.index' %}
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="display-4">Mes Catégories</h1>
        <a href="{{ path('admin.category.new') }}" class="btn btn-primary d-flex items-center p-3">
            <i class="ri-add-line mr-2"></i>
            Ajouter un une Catégorie
        </a>
    </div>
{% endif %}

{% if app.request.attributes.get('_route') == 'admin.tricks.index' %}
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="display-4">Mes Tricks</h1>
        <a href="{{ path('admin.trick.new') }}" class="btn btn-primary d-flex items-center p-3">
            <i class="ri-add-line mr-2"></i>
            Ajouter un Trick
        </a>
    </div>
{% endif %}

